$(document).ready(function(){

  /* start the header */
	$(".head").css({
   
     height: $(window).height(),

	});
	/* end the header */
   /* start wow.js */
    
    new WOW().init();
    
  /* end wow*/
});